pipeline { 
    agent {
        label {
	     label "vm-agent"
            customWorkspace "/home/ubuntu/Jenkins/pipleines/maven-job"
        }
    }
    stages { 
        stage('Build') { 
            steps { 
               sh 'mvn clean package' 
            }
        }
