pipeline { 
    agent {
        label {
	     label "vm-agent"
            customWorkspace "/home/ubuntu/pipleine"
        }
    }
    stages { 
        stage('Build') { 
            steps { 
               sh 'mvn clean package' 
            }
        }
  }
}
